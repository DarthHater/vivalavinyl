'use strict'
/* global describe, it */

const assert = require('assert')

describe('ThreadController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ThreadController'])
  })
})
